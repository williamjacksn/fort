version: '3.8'

services:
  fort-dev:
    image: docker.pkg.github.com/williamjacksn/fort/fort-dev
    build: .
    volumes:
      - ./:/fort

  py-build:
    image: docker.pkg.github.com/williamjacksn/fort/fort-dev
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command:
      - setup.py
      - sdist
      - bdist_wheel

  twine-upload:
    image: docker.pkg.github.com/williamjacksn/fort/fort-dev
    working_dir: /workspace
    volumes:
      - ./:/workspace
    entrypoint:
      - /usr/local/bin/twine
    command:
      - upload
      - --skip-existing
      - --verbose
      - dist/*
    environment:
      TWINE_PASSWORD:
      TWINE_USERNAME:

  cleanup:
    image: docker.pkg.github.com/williamjacksn/fort/fort-dev
    entrypoint:
      - rm
      - -rf
      - build
      - dist
      - fort.egg-info
